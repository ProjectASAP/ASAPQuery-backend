//! Control-plane compatibility surface for the shared catalog wire contract.
//!
//! The authoritative catalog is constructed and published by the control
//! plane, while its immutable contract is shared with every consumer.

pub use asap_types::summary_catalog::*;
